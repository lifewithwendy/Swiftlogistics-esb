server:
  port: 8084

camel:
  component:
    rabbitmq:
      hostname: localhost
      port: 5672
      username: guest
      password: guest

logging:
  level:
    root: INFO
    org.apache.camel: INFO
